export const DUPLICATE_HOTEL_SETTLEMENT_CODE = "DUPLICATE_SETTLEMENT" as const;

export const DUPLICATE_HOTEL_SETTLEMENT_MESSAGE =
  "A settlement record already exists for this hotel and month." as const;
